-- ============================================================
-- UC-003 喷淋降尘系统：初始数据脚本
-- 清空喷淋操作记录，保证演示时所有喷淋设备初始为「关闭喷淋」状态
-- （开启按钮可点、关闭按钮禁用；/api/spray/status 的 spraying 全为 false）
-- 可重复执行；如需保留历史联动记录可只执行 DELETE 部分
-- ============================================================

DELETE FROM t_spray_record;
ALTER TABLE t_spray_record AUTO_INCREMENT = 1;