# 设备通信模拟平台 · 手动推送使用教程

> 适用页面：系统内「设备通信模拟平台」（左侧菜单 → 设备通信模拟平台，路径 `/iot`）
> 适用版本：2026-08 演示版（含手动推送功能）

## 一、这个功能是干什么的

系统里塔吊 / 升降机 / 环境监测的数据，正常由「模拟器」（`simulator` 目录的 `node app.js`）
通过 TCP 长连接自动上报。**手动推送**让你（演示人）不依赖模拟器，直接在页面上：

- 自己编写一条设备报文（数据随便改）
- 点一下**单次推送**，立即上报一条
- 点**周期推送**，按你设定的秒数持续上报
- 点**停止推送**，随时停

推送的数据和真实设备上报走的是**同一条链路**：落库 → 写入实时数据 → 触发阈值告警 →
WebSocket 推送 → 塔吊/升降机/环境监控页实时更新。所以**你可以用它在演示时"制造"告警**。

## 二、页面操作步骤

页面在「数据链路」卡片下方，标题为 **"✍️ 手动推送（演示人可自定义报文内容）"**。

### 1. 选择设备
「模拟设备」下拉框选择要模拟的设备：

| 设备编码 | 设备名称 | 类型 |
|---|---|---|
| TC-001 | 1#塔吊 | 塔吊 |
| TC-002 | 2#塔吊 | 塔吊 |
| LFT-001 | 1#施工升降机 | 升降机 |
| ENV-001 / ENV-002 | 环境监测站1 / 2 | 环境监测 |

选中后「报文内容」会自动填入该设备的模板（JSON 格式，可随意修改数值）。

### 2. 修改报文内容（关键）
报文是 JSON 对象，直接改数字即可。例如塔吊模板：

```json
{
  "load": 3.5,          // 吊重(吨)：改成 9.0 以上 → 触发吊重警报
  "radius": 25.0,       // 幅度(米)
  "wind_speed": 6.5,    // 风速(m/s)：改成 19 以上 → 触发风速警报
  "height": 45.0,       // 吊钩高度(米)
  "angle": 120.5        // 回转角度(度)
}
```

环境站模板：

```json
{
  "PM2.5": 62.5,        // 改成 160 以上 → 触发 PM2.5 警报（>150 警报级）
  "PM10": 118.0,
  "噪声": 66.2,         // 改成 92 以上 → 触发噪声警报
  "温度": 28.6,
  "湿度": 61.0,
  "风速": 5.2
}
```

升降机模板：

```json
{
  "load_weight": 1200,     // 载重(kg)：改成 2000 以上 → 载重警报
  "person_count": 6,       // 人数：改成 9 以上 → 超员告警
  "height": 42.5,
  "wind_speed": 5.0,
  "direction": 1,          // 1-上升 2-下降
  "door_front": 1,         // 前门 1-开 0-关
  "door_back": 0           // 后门：与 front 同时为 1 → 双门同开告警
}
```

> 也可以点「快捷模板」标签（塔吊/升降机/环境）快速切换模板。

### 3. 三个按钮
- **单次推送**：立即上报一次当前报文（右上角「上报记录」表格能看到新记录）。
- **周期推送**：按「周期(秒)」设定的间隔持续上报（默认 5 秒）。同一设备重复点击会先停掉旧的再重新开始。
- **停止推送**：停止当前所选设备的周期推送。

正在周期推送的设备会在卡片右上角显示 `⏱ 设备编码 周期推送中` 标签，每 3 秒自动刷新。

## 三、演示建议（制造告警）

1. **吊重超限**：选 TC-001，把 `load` 改成 `9.5`（额定 8t），点「单次推送」→ 塔吊监控页立即显示告警，右上角铃铛响。
2. **PM2.5 超标联动喷淋**：选 ENV-001，把 `PM2.5` 改成 `160`，点「周期推送」→ 环境监测页超标 + 喷淋降尘自动联动开启。
3. **升降机超员/双门**：选 LFT-001，把 `person_count` 改成 `9`，或 `door_front`/`door_back` 都设 1，点「单次推送」→ 升降机监控页告警。
4. **正常演示**：用模板默认值周期推送，各监控页数据平滑跳动。

## 四、常见问题

- **推送了但监控页没变？** 检查推送的设备编码是否与监控页设备一致（TC-001/TC-002/LFT-001/ENV-001/ENV-002），以及「上报记录」里有没有新记录。
- **报文内容报"不是合法 JSON"？** 检查引号/逗号是否完整，可用快捷模板重置后改数字。
- **周期推送停不下来？** 在页面选中该设备 → 点「停止推送」；或重启后端（内存中的推送任务随之清除）。
- **数值推送后想恢复自动模拟？** 手动推送不会影响 `simulator` 的自动上报，两者数据并存，后上报的覆盖最新值；无需任何恢复操作。

## 五、技术说明（给开发/维护）

- 后端接口：`POST /api/iot/sim/push`（单次）、`POST /api/iot/sim/start`（周期，参数 intervalSec）、`POST /api/iot/sim/stop`、`GET /api/iot/sim/status`
- 实现：`IotDataService.simPushOnce()` 构造报文直接调 `handleReport()`，与 TCP 上报同一处理链路（落库 t_iot_data + 写 t_realtime_data + WS 广播 + MockDataScheduler 阈值告警）
- 周期推送用 `ScheduledExecutorService` + `Map<deviceCode, ScheduledFuture>`，同一设备重复 start 先 stop 旧的；后端重启即清空
